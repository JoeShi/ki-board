---
inclusion: always
---

# 自动编译与烧录规则

## 编译验证
修改 `src/`、`include/` 或 `lib/` 下的代码文件后，自动运行：
```bash
pio run -e esp32-s3
```

若编译失败，根据错误信息修复代码后重试，直到编译通过或明确需要用户决策时再询问。

## 烧录
编译通过后，自动烧录到开发板：
```bash
pio run -e esp32-s3 --target upload
```

若烧录失败（串口未连接、权限不足等），提示用户检查硬件连接，不要反复重试。
